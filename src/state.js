/**
 * Estado global compartilhado entre todos os módulos.
 * Importar com: import { S } from '../state.js'
 */
export const S = {
  token:    null,
  schoolId: null,
  apiBase:  '',
  classes:  [],
  teachers: [],
};
